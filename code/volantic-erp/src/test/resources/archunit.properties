# Greenfield modulith: layers (e.g. ..api..) appear module by module. Forward-looking architecture
# rules may therefore (still) match no class without breaking the build. Once the layer exists, the
# rule kicks in automatically.
archRule.failOnEmptyShould=false
